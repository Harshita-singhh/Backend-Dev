# 1. Go to the log directory
cd /var/log/app

# 2. List all files (including hidden) with human-readable sizes
ls -lah

# 3. Show the last 20 lines of error.log
tail -n 20 error.log

# 4. Search for "error" (case-insensitive) in all .log files
grep -i "error" *.log

# 5. Save the search output to error_report.txt
grep -i "error" *.log > error_report.txt
